name: Deploy VSIX Release

on:
  release:
    types:
      - published

permissions:
  contents: write

env:
  MANIFEST_PATH: BiakVisualStudioExtension/source.extension.vsixmanifest

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout by tag
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: Restore NuGet packages
        run: nuget restore

      - name: Resolve release version from tag
        id: release_meta
        shell: pwsh
        run: |
          $tag = "${{ github.event.release.tag_name }}"
          $version = $tag.TrimStart('v')

          if ($version -notmatch '^\d+\.\d+\.\d+(\.\d+)?$') {
            throw "Tag '$tag' is invalid for VSIX. Use v1.2.3 or v1.2.3.4."
          }

          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          "release_tag=$tag" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          "release_name=VSIX $version" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Set VSIX manifest version
        shell: pwsh
        run: |
          [xml]$manifest = Get-Content -Path "${env:MANIFEST_PATH}"
          $namespaceUri = "http://schemas.microsoft.com/developer/vsx-schema/2011"
          $ns = New-Object System.Xml.XmlNamespaceManager($manifest.NameTable)
          $ns.AddNamespace("vsx", $namespaceUri)
          $identity = $manifest.SelectSingleNode("/vsx:PackageManifest/vsx:Metadata/vsx:Identity", $ns)
          if (-not $identity) { throw "Identity node not found in VSIX manifest." }
          $identity.SetAttribute("Version", "${{ steps.release_meta.outputs.version }}")
          $manifest.Save("${env:MANIFEST_PATH}")

      - name: Build solution (Release)
        run: msbuild biak-visual-studio-extension.slnx /p:Configuration=Release

      - name: Locate VSIX output
        id: vsix
        shell: pwsh
        run: |
          $vsix = Get-ChildItem -Path "BiakVisualStudioExtension/bin/Release" -Filter "*.vsix" -Recurse |
            Where-Object { $_.Name -notlike "*.vsixmanifest" } |
            Select-Object -First 1
          if (-not $vsix) { throw "VSIX package not found under BiakVisualStudioExtension/bin/Release." }
          "path=$($vsix.FullName)" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Upload VSIX to existing GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.release_meta.outputs.release_tag }}
          files: ${{ steps.vsix.outputs.path }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
